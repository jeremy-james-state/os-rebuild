# RESUME HERE

> Auto-generated by `/handoff` — overwrite each session. Do not edit by hand.
> The dated history lives in [`../record/handoffs/`](../record/handoffs/).

## Current state (one line)
The deterministic harness layer is taking shape — `/incident` + the Investigator agent are on main as a governed candidate, and `/handoff` is now reinstated to persist handoffs as **tracked markdown** in `record/handoffs/` (closing the persistence gap the previous handoff flagged).

## Mission
A governed operating system ("the harness") for Claude Code that can eventually rebuild itself, bounded. Core principle: **"bound to be free"** — lock the governance layer before the operational layer. v1 failed because it was built before it was defined; v2 defines first, enforces, then delivers.

## Working model
Claude (web session) orchestrates; work lands via PRs that the **human reviews → approves → merges** (Claude authenticates *as the human*, so it cannot self-approve). The `doctor` check in `.github/workflows/ci.yml` is **required / merge-blocking**. New executing code enters as a **candidate** in `harness/sandbox/` + a contract + a row in `governance/candidates.md` — never straight into `manifest.json`/`harness/` (harness-admission: Located · Registered · Contracted · Green · Admitted).

## What's on main
- **Governance decisions:** component-model, authority, write-zones, data-layer (four-tuple `session·run·call·branch`, supersede-not-delete), governability, identity, self-governance, repo-structure, **platform-home** (OPEN), **manifest-registry** (registry.json rows + manifest.json rails, merged by doctor — gov-038).
- **Enforcement:** doctor (merge-blocking), governance-check (ledger integrity + every workflow declared in `environment.json`, fail-closed), structure-check.
- **The deterministic incident loop:** `/incident` + the **Investigator** agent (first governed candidate, off the main channel; `incident.mjs isComplete` = the exit).
- **Markdown data layer (interim, human form):** `record/incidents/` and now `record/handoffs/` — tracked markdown filed by hand until the Data Layer + orchestrator route streams automatically.
- **signal-ledger** candidate (capture-only).

## In flight
- **Branch `claude/handoff-markdown`** — reinstates `/handoff`; stores handoffs as tracked markdown in `record/handoffs/` (+ this file as the latest render); retires the gitignored `record/handoffs.jsonl` surface. PR pending review.

## Next steps
1. Review + merge the handoff-markdown PR.
2. (User, via GitHub UI) delete the stale merged branches — branch deletion is blocked from this environment.
3. Form the **Data Layer** (SQLite projection of `record/*.jsonl` truth) + the orchestrator/dispatcher — the brick that turns the interim markdown folders into automated routed streams and gives a true off-channel runtime home (platform-home, OPEN).

## Gotchas / constraints
- **Runtime jsonl under `record/` is gitignored** (durable home = Data Layer); only `governance-ledger.jsonl` is tracked. That's why incidents and handoffs are filed as **tracked markdown**, not jsonl.
- **Ledger IDs are global and collide on parallel branches** — union-merge + renumber at merge time (`gov-YYYY-MM-DD-NNN`).
- **Branch deletion fails from this environment** — the user deletes merged branches via the GitHub UI.
- The `doctor` check must be green before a PR can merge.
