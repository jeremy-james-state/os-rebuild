# RESUME HERE

> Auto-generated by `/handoff` — overwrite each session. Do not edit by hand.
> The dated history lives in [`../record/handoffs/`](../record/handoffs/).

## Current state

On `main` @ `1a85a25` (release 0.13); working tree clean except **two new untracked spec docs** — this
session's only output. No open PRs. This session designed, adversarially reviewed, hardened, and got an
**APPROVED, autonomy-ready execution plan** for the OS/harness **reshape + sealed runtime**. **Nothing has
been migrated.** The plan is packaged for a **fresh session to RUN** (context here was too high to execute).

Specs written this session:
- `docs/superpowers/specs/2026-07-01-os-reshape-execution-plan.md` — **the plan to RUN (START HERE)**
- `docs/superpowers/specs/2026-07-01-os-harness-architecture-design.md` — architecture + vocabulary

## Mission

Reshape `harness/` **in place** into a legible six-concept OS (`harness · apps · skills · governance ·
record · docs` + `.system/`), **decouple lifecycle state from file location**, and add a **sealed, versioned
runtime** the session boots from (separate from the messy workbench). Governing standard: **test-first,
evidence-gated, 5/5 only when demonstrated, no silent failures.** Evidence and actual experience trump all.

## Working model (Autonomy Contract — for the RUN session)

Run **unattended, end-to-end**: never stop to ask (unforeseen choices → best default + logged note);
**fix-forward** (debug+fix+retry, never halt); **commit+push after every green milestone** (crash-safe,
monotonic); **gates async** (recorded/pushed, never a live block); Phases 0→4 with **deterministic per-phase
gates**; stop only when all green + pushed. **The ONE setup step: bypass permissions on + git push working.**

## What's on main

The working signal loop (session-feedback → orchestrator → classifier/estimator/tracer/loop-store) +
confinement + the four checks (doctor · governance-check · structure-check · no-ghost-agent), all as
**candidates under `harness/sandbox/`**. Release **0.13**; CI merge gate (**≥100 checks**: 4 checks + all
`*.test.mjs`). Live dashboard: `https://web-lemon-ten-15.vercel.app` (fed by `scripts/sync-supabase.mjs`).

## In flight

Nothing executing. The execution plan is **approved + tracked**; it awaits a fresh session to run it.

## Next steps (the RUN session — read the execution plan FIRST)

1. Open `docs/superpowers/specs/2026-07-01-os-reshape-execution-plan.md` → **START HERE**.
2. Confirm the ONE setup step (bypass permissions + git push).
3. Execute **Phases 0→4**, gating each on the **§D2 deterministic eval battery**; **re-run the three
   adversarial reviews' checks**; **RED-first fault injection**; commit+push each green milestone.
4. Honour the **locked rulings**: branch `os-reshape` + **CI-gated auto-merge**; **one additive**
   governance-ledger entry; **availability-only O4**; **autonomous adoption on green**.
5. Adopt **autonomously** once the FULL battery is green **UNDER LOCKDOWN** (controls flipped back to
   fail-closed); push the evidence pack; **notify async**. Revertible via the rollback drill (R1).

## Gotchas (non-negotiables + the traps the three reviews found)

- **NN1** — the `🔁` signal loop must fire on **EVERY** message: preserve `session-feedback` byte-identical;
  **verify live** post-migration (next real prompt must show the trace).
- **NN2** — dashboard **exactly as is**: **do NOT move `web/`** (deploy-pinned at root). Also **`.github/`
  stays at root** (moving it silently kills CI) and **`render.mjs` stays in `harness/`** (moving it breaks
  the doctor's md-sync forever).
- **Keep `state/` at root by default** (moving it orphans `.gitignore` patterns + splits `os.db`, which is
  written by both loop-store and signal-ledger).
- The **sealed snapshot is a REAL refactor**, not a repoint: build a **boot-root resolver** (kill every
  `resolve(HERE,'..','..','..')` in runtime read paths), a **cut-then-verify** gate, bundle the doctor's
  dependency chain, and keep `record/`+`state/` **outside** the read-only snapshot.
- **Version re-cut + re-tag** in the migration commit (else `checkVersionBumpOnChange` goes blind on moved
  paths). The `manifest`+`registry` **merge** needs a **union schema** + `checkSchemas`/`render`/`tracer`
  rewrites (not a "clean consolidation").
- **Harden `structure-check` + `no-ghost-agent` to fail-closed** (today they can pass silently). Prove all
  controls **UNDER LOCKDOWN** (build-relaxed → flipped back, evidenced). The new session **re-earns green
  with evidence — it does not inherit it.**
