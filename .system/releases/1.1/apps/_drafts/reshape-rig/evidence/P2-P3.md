# P2 + P3 evidence — sealed runtime · concurrency · TestFlight

> Phases 2–3 of [the execution plan](../../../../docs/superpowers/specs/2026-07-01-os-reshape-execution-plan.md).
> Gates: §C matrix RED→GREEN · O1 under snapshot boot · cut-then-verify refuses bad snapshots ·
> X1–X3 green · channel toggle loud · promote/revert atomic · peer-reviewed.

## P2 — sealed runtime (release 0.18 → 0.19)

- **`.system/bin/os-publish.mjs`**: assembles the closure image (`harness` + `apps` +
  `governance/checks` + own pin), **cut-then-verify INSIDE the image** (doctor + no-ghost +
  hostile boot smoke from an outside cwd with a hermetic record), then atomically repoints.
  `--verify` refuses torn snapshots explicitly; `--boot-check` prints the `🖥` banner.
- **S-matrix S0–S8 all GREEN** (written RED-first at P0): isolation boot with the ENTIRE
  workbench renamed away; gutted-snapshot loud failure; blank-version refusal; torn-snapshot
  verify refusal; stale-pointer refusal; channel-mismatch refusal; version stamp under sealed
  boot; record/+state/ external.
- **OS_ROOT indirection** threaded through orchestrator/tracer/loop-store/reconciler.
- **Symlink-proof CLI main-guards repo-wide** — S1's RED-first run caught our own `current`
  symlink re-triggering the P0 no-op class; killed in all 20 entry points (`cliInvoked`).
- **C6 fabricated-clean-bill**: dead (fail-loud doctor handler, landed with P1 review).

## P3 — concurrency + TestFlight (release 0.19)

- **Lock-steal killed** (X2b RED-first, empirically reproduced at P0): live holders are NEVER
  robbed; dead/stale locks broken via **claim-then-break** (rename-claim + re-verify + restore);
  release is **inode-guarded**; metadata written through the fd. A live lock outlasting the wait
  budget **fails CLOSED with a durable drop record** — no silent loss, ever. X2a stress 6/6.
- **First fix attempt had its own race** (blind unlink on ENOENT deleted a NEW holder's lock —
  caught by X2a flaking 2/5 in-battery) — replaced by the race-free protocol above. The rig
  caught both the original bug and the bad fix.
- **harness-lock wired LIVE** (PreToolUse) + functional collide leg green (X1); spine files
  lock as `harness-spine`.
- **Publish serialization**: O_EXCL publish lock (X3) + **rename-aside reseal** (no destruction
  window; crash-repairable; restore-on-verify-failure) + dir-pointer early refusal.
- **Stable launcher `.system/bin/os-boot.mjs`**: channel-file boots from the sealed image,
  data layer stays in the workbench (env), loud fail-open (⚠ context injection / ⛔ banner),
  `next` = TestFlight (boots with a 🧪 stderr banner). Promote/revert = atomic symlink swap.
  Smoke-verified end-to-end: sealed session-feedback + statusline through the launcher, and a
  loud refusal on `channel=next` with nothing published.

## P2 peer review — 4 confirmed (0 refuted), all folded in (workflow `wf_6110e12e-ea3`)

1. **[high] `--force` reseal destruction window** — `current` could dangle across an O(tree)
   delete; a crash bricked the sealed OS. → rename-aside + restore-on-failure (both halves).
2. **[high] verify-at-publish ≠ verify-at-boot** — a snapshot committed after its tag inherits
   parent-repo git context; a byte-identical image flips from 0 errors to 18
   `version-bump-required` (reproduced). → root-is-toplevel guard on `checkVersionBumpOnChange`;
   discipline: tag and snapshot land in the SAME commit.
3. **[med] dir-form pointer EISDIR crash after destructive steps** → early refusal.
4. **[med] grep-gate scanned immutable sealed history** (first future pattern = permanent
   failure) → sealed-releases exemption.
   Plus two **verified-clean baselines**: closure completeness (traced import-by-import,
   empirical: doctor 0 errors inside the image) and smoke-env hermeticity.

Also discovered by the rig this phase: **rename-invariance is false** — verification now
re-runs at the FINAL location before any repoint (what gets verified is what gets booted).

## Battery

**243 tests / 241 pass.** The only reds are the two P4 lockdown targets (W1 out-of-repo write
blocked under lockdown; W4 adopted state boots sealed). F5 golden master identical through
both phases. `🖥 OS v0.19 booted · channel current · 57 components`.
