# agents/ — agent definitions as data (role, prompt, lifecycle). The runner code lives in harness/.

- [`investigator.md`](investigator.md) — *what information best reduces uncertainty?* First application: investigate + fill incidents. Candidate; dispatched by `/incident`; exit = `isComplete`.
