# Handoff — 2026-07-01

- **id:** handoff-2026-07-01
- **session:** c7f29449-session
- **git_head:** 8676e6f

> One file per handoff (append-only history). The always-current render of the **latest**
> handoff lives at [`../../docs/RESUME-HERE.md`](../../docs/RESUME-HERE.md) — read that first.

## Current state (one line)

The harness/OS version-control + governance system is built, live, and self-governing — harness **v0.12**, `main` locked behind a CI merge-gate, everything landed through 12 gated PRs.

## Mission

os-rebuild is the rebuilt "Harness" OS (governance / execution / data layers + a wired signal loop). This phase closed the "changes vanish silently" gap: every change to the harness/OS is now versioned, checked, traceable, and gated — so the owner governs by **observing, not reviewing**.

## Working model

Local is canonical; `main` is protected by the `main-branch-governance` ruleset. No direct pushes — every change flows **branch → PR → CI (four checks + all tests, required) → auto-merge on green**. The AI opens + merges conforming PRs (non-admin); the human is the observer. Admin (ruleset edits) stays with the human.

## What's on main

- **Versioning:** per-component contract-keyed semver + deliberate harness releases (v0.8→v0.12, git-tagged); 55 components, all versioned.
- **16 enforcement checks** (doctor / governance-check / structure-check / no-ghost): mandate (code-changed ⇒ version-bump), release/index/changelog sync, self-admission guard, conflict-marker. 193+ tests.
- **Governed components:** `incident` + `handoff` (handoff can only save to `record/handoffs/` — code + test + gate guaranteed). `sandbox → candidate` state reframe.
- **Built but DORMANT:** confinement hardening (fail-closed `decideStrict` + Tier-1 seatbelt profile); per-component write-lock + branch-discipline guard.
- **Observability:** version-stamped Supabase `osr_loop_events` + Vercel dashboard, kept fresh by a **local launchd sync** (reads `.supabase-key`, every 30 min).
- CI merge-gate active + required; **auto-merge-on-green** enabled; `scripts/` now in the CI test net (PR #12).

## In flight

- None open — `main` clean at PR #12 (`8676e6f`).

## Next steps

1. **Fix the flaky test (priority)** — `harness/sandbox/session-feedback/session-feedback.test.mjs:66` ("os: command emits a real block decision") runs the real doctor (~7s) and ~50% returns `undefined` instead of `'block'`. It's in `harness/` so always in CI; now it threatens every PR's auto-merge. Make it deterministic (stub/inject the doctor or fix the timing) — a real fix, not a skip.
2. **Level-2 (optional):** promote operational scripts (`sync-supabase` = the Data-Layer transport; `data-lock` = a control) into the harness as governed components; leave dev-tooling in `scripts/` (now CI-tested).

## Gotchas / constraints

- **Standing rule:** the AI must NOT hold GitHub admin — a control that exempts the enforcer isn't a control. Non-admin token; hand the human the click for ruleset edits.
- The dashboard sync is **local only** (`record/*.jsonl` is gitignored, so CI can't sync it); driven by `<repo>/.supabase-key` (gitignored) + a launchd job.
- Adding a component triggers the **release-pin interlock** → cut a new release (bump `harnessVersion` + `harness/releases/<v>.json` + regenerate index/changelogs), or `checkReleaseConsistency` fails.
- Lesson from the `const URL`-shadow bug: ungoverned/untested executing code (it was in `scripts/`, outside CI) hid a real bug — now closed by pulling `scripts/` into CI.
