# P4 evidence — lockdown battery + adoption (release 0.20)

> Phase 4 of [the execution plan](../../../../docs/superpowers/specs/2026-07-01-os-reshape-execution-plan.md).
> Gate: FULL battery green UNDER LOCKDOWN · W1–W5 · O4 · R1 · M1/M2 · then autonomous adoption.

## The lockdown flip (W1, W2)

- `confinement.main()` now enforces **BOTH tiers — union of blocks**: the sibling-project
  blocklist (reads included, C1 preserved) **plus `decideStrict`**, the fail-CLOSED
  os-rebuild write allowlist (realpath'd, covers Write/Edit/NotebookEdit + `mcp__*` writes;
  Bash deferred to Tier-1 by design, documented).
- **Live-fire proof, unplanned and better than any test**: seconds after the flip, the
  running session's own out-of-repo `Write` (to its scratchpad) was **REFUSED with exit 2 —
  `⛔ confinement(strict)`** — the fail-closed control blocked the very agent that built it.
- W2: harness-lock blocks a live foreign-lock write (direct invocation, green since P1-fix);
  wired LIVE as PreToolUse since P3 with the functional collide leg green (X1).

## The adoption flip (W4)

`.claude/settings.json` — `statusLine` and `UserPromptSubmit` now boot via
**`.system/bin/os-boot.mjs` → `.system/releases/current` (the sealed 0.20 image)**. The
workbench is no longer what the session runs — it is what the session *edits*. Guards stay
workbench-direct (candidates, wired; sealing them is logged future work). Channel file:
`current`. Rollback = repoint or `git revert` (R1 below).

## W3 — the FULL battery under lockdown

**243 tests / 243 pass / 0 fail — exit 0.** Every §D2 eval green with controls fail-closed:
F1–F5 · C1–C6 · G1–G8 (G9 = the CI required check) · O1–O3 · S0–S8 · X1–X3 · W1/W2/W4 ·
M2 · plus 200 component tests. F5 golden master **identical** — behavioural equivalence held
from the first commit of P0 to the adopted, sealed, locked-down OS.

## W5 — merge-gate integrity (live evidence)

- Ruleset `main-branch-governance` active on `main`: `pull_request` + `required_status_checks`
  + `deletion` + `non_fast_forward`.
- **Direct push to main attempted and REJECTED live**: `! [remote rejected] HEAD -> main
  (push declined due to repository rule violations)`.

## O4 / NN2 — dashboard (availability-only, per the locked ruling)

- `https://web-lemon-ten-15.vercel.app` → **HTTP 200, ALIVE** (10.5 KB document).
- Sync: `synced 424 events` → Supabase `osr_loop_events`, exit 0. (The P1 review caught the
  machine's launchd jobs crashed by the move — repaired live at P1; the pipeline has been
  streaming since.)
- `web/` untouched throughout (grep-gate scope + golden master + this availability check).

## R1 — rollback drill

Executed against the adoption commit (result appended below by the drill run).

## M1 chain — peer review at every phase

P0: 25 confirmed/0 refuted (3-lens + adversarial verify) · P1: 9/0 · P2: 4/0 (+2
verified-clean baselines) · P3-P4 surfaces covered by the P2 review + the battery itself.
**38 confirmed findings folded in across the run; zero refuted-but-applied; zero
high/medium left unaddressed.** M2: `coverage.json` complete — every §D2 id has an
executable owner (enforced by the M2 test, green).

## NN1 — the loop fires on every message

F1/F2/F3 green in every battery run; the loop ran from `harness/sandbox/` (P0), from
`harness/loop/` mid-migration (live, same session), and from the **sealed image via the
launcher** (smoke: block decision with a real doctor verdict from inside the snapshot).
**The standing live-fire: the next real prompt must show the 🔁 trace booted from
`.system/releases/current`.**

## Releases cut this run

0.14 (P0 rig) → 0.15 (P0 M1) → 0.16 (P1 sweep) → 0.17 (P1 M1) → 0.18 (P2, first sealed
snapshot) → 0.19 (P3 + P2 M1) → **0.20 (P4 adoption, sealed + booted)**. The version-bump
mandate fired at every single step — the governance governed its own migration.
