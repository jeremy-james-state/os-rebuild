# P1 evidence — the in-place reshape

> Phase 1 of [the execution plan](../../../../docs/superpowers/specs/2026-07-01-os-reshape-execution-plan.md).
> Gate: grep-gate=0 · 4 checks green · golden-master identical · P1-targets green · battery ≥ baseline · peer-reviewed.

## What moved (commit 2c9c7fa, 158 files, all git renames)

`harness/sandbox` → `harness/loop/` (8 loop parts + router + LOOP.md) + `harness/guard/`
(confinement, harness-lock) + `apps/_drafts/` (handoff, incident, investigator, pipeline,
reshape-rig) · `governance/enforcement` → `governance/checks` · pins → `.system/releases/`
(render.mjs stayed) · `scripts` → `.system/sync` (depth-corrected) · type-folders collapsed ·
`skills/` + `.system/{bin,archive,state}` seeded · registry merged INTO the manifest (union
schema, one spine file). Pinned: `web/`, `.github/`, `.claude/`, `state/`.

## Gate results

- **grep-gate: 0 hits across 6 armed patterns** (was 67 at arming — every stale ref hunted down).
- **Four checks green** — locally AND on a fresh Linux CI checkout (the checks steps pass;
  CI's only failures are the designed phase-target REDs).
- **F5 golden master: IDENTICAL (normalized) across the whole reshape** — the
  behavioural-equivalence proof, maintained through the merge, the moves, the hardening,
  and two release cuts.
- **Battery 243 tests / 228 pass**: all 5 P1-targets flipped GREEN (grep-gate armed+zero,
  O1b componentVersion, O1c loud-on-broken-manifest, G4 structure fail-closed, G5b no-ghost
  fail-loud) + **C6 flipped green EARLY** (doctor-handler fail-loud landed with the review
  fixes). Remaining 15 fails = 14 P2/P3/P4 targets + the G6 tag-window artifact (heals at
  the 0.17 tag, verified pattern from 0.16).
- **NN1 live**: the session's own hooks ran from the new paths mid-migration (confinement
  blocked from `harness/guard/` during the sweep; session-feedback CLI verified from
  `harness/loop/`; the next real prompt's 🔁 is the standing live-fire).

## M1 peer review (the gate) — 9 confirmed, 0 refuted, all folded in

3 lenses (silent-breakage · hardening · equivalence), every finding adversarially verified
(workflow `wf_6a484b77-aa0`):

1. **[high] Machine launchd sync jobs dead** (both plists → deleted `scripts/` path;
   MODULE_NOT_FOUND every 15/30 min; dashboard pipeline down). **Repaired live**: installer
   re-run from `.system/sync/`, redundant job removed, `synced 412 events` confirmed,
   exit 0. Reinstall note added to `.system/sync/README.md`.
2. **[high] Contract validation silently shrank** — `checkSchemas` scanned only `harness/`;
   the 4 moved `_drafts` contracts dropped out. Widened to `apps/` + regression test.
3. **[high] Doctor handler fabricated `ok:true` on a crashed doctor** (status ignored,
   empty stdout → clean bill on the ENFORCED path). Fail-loud rewrite → C6 green.
4. **[med] Doctor's naive `file://` main-guard** could exit 0 silently (space/symlink
   paths) — the exact no-op the rig hit at P0. pathToFileURL guard on doctor +
   orchestrator + session-feedback.
5. **[med] Spine files unlocked** — `componentOf('harness/manifest.json') → null → allow`;
   the most contended file in the one-spine design. `harness-spine` pseudo-component + test.
6-7. **[med] Dead doc paths** (installer usage line, web/README sync command) — fixed.

## Releases

0.16 (the sweep, 17 components bumped) → 0.17 (review fixes: orchestrator 0.1.3,
harness-doctor 0.3.2, session-feedback 0.1.2, harness-lock 0.2.2). Version-bump mandate
fired correctly at every step — the control governed its own migration.
